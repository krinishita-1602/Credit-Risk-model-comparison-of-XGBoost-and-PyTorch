# Credit-Risk-model-comparison-of-XGBoost-and-PyTorch
The dataset used is the German Credit dataset loaded from UCI.
The code performs data preprocessing (one-hot encoding and scaling).
It builds a neural network using PyTorch.
It also trains a model using XGBoost.
Finally it compares metrics like accuracy, precision, recall, and F1-score.
